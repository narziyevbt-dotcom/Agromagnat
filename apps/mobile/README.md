# Agromagnat — mobile

Flutter app for the Agromagnat agricultural marketplace. Android and iOS from
one codebase, Uzbek UI throughout.

```bash
flutter pub get
flutter run          # runs on mock data — no backend needed
flutter test
flutter analyze
```

Architecture, design tokens, platform notes and what is built so far:
[`docs/MOBILE.md`](../../docs/MOBILE.md).
